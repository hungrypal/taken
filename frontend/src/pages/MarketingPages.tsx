// Public pages use the warm, agriculture-first visual language of Agrishield
// while retaining TerraScore's product messaging and React routes.
import { ArrowRight, BarChart3, BrainCircuit, CheckCircle2, CloudSun, Leaf, Mail, MapPinned, Menu, ShieldCheck, Sprout, X } from "lucide-react";
import { useState } from "react";
import { Link } from "react-router-dom";

const nav = [{ to: "/about", label: "About" }, { to: "/features", label: "Features" }, { to: "/faq", label: "FAQ" }, { to: "/contact", label: "Contact" }];

function PublicShell({ children }: { children: React.ReactNode }) {
  const [menuOpen, setMenuOpen] = useState(false);
  return <div className="min-h-screen bg-[#f7faf7] text-slate-800"><header className="sticky top-0 z-30 border-b border-emerald-100 bg-white/95 shadow-sm backdrop-blur"><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5"><Link to="/home" className="flex items-center gap-3"><span className="rounded-2xl bg-emerald-600 p-2.5 text-white shadow-lg shadow-emerald-600/25"><Leaf size={22}/></span><span><span className="block text-xl font-bold text-slate-800">TerraScore</span><span className="block text-[11px] text-emerald-700">Smart farming solutions</span></span></Link><nav className="hidden items-center gap-1 lg:flex">{nav.map((item) => <Link key={item.to} to={item.to} className="rounded-lg px-4 py-2 text-sm font-medium text-slate-600 transition hover:bg-emerald-50 hover:text-emerald-700">{item.label}</Link>)}<Link className="ml-3 rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-semibold text-white shadow-md shadow-emerald-600/20 transition hover:bg-emerald-700" to="/login">Sign in</Link></nav><button className="rounded-lg p-2 text-slate-700 hover:bg-emerald-50 lg:hidden" onClick={() => setMenuOpen(!menuOpen)} aria-label="Toggle navigation">{menuOpen ? <X/> : <Menu/>}</button></div>{menuOpen && <nav className="border-t border-emerald-100 bg-white px-5 py-3 lg:hidden">{nav.map((item) => <Link key={item.to} to={item.to} onClick={() => setMenuOpen(false)} className="block rounded-lg px-4 py-3 text-sm font-medium text-slate-700 hover:bg-emerald-50">{item.label}</Link>)}<Link to="/login" onClick={() => setMenuOpen(false)} className="mt-2 block rounded-lg bg-emerald-600 px-4 py-3 text-center text-sm font-semibold text-white">Sign in</Link></nav>}</header>{children}<footer className="border-t border-emerald-100 bg-white px-5 py-9 text-center text-sm text-slate-500">© {new Date().getFullYear()} TerraScore · Agricultural intelligence for confident credit decisions.</footer></div>;
}

const features = [
  [BrainCircuit, "Explainable scoring", "Turn model outputs into practical credit and risk indicators."],
  [MapPinned, "Farm-aware insights", "Save field locations and connect every prediction to a farm."],
  [CloudSun, "Climate context", "Bring vegetation, temperature, and forecast signals into one view."],
  [BarChart3, "Clear analytics", "Follow prediction trends and risk patterns as your farm data grows."],
  [ShieldCheck, "Secure by default", "Protected records and account-scoped farm intelligence."],
  [Sprout, "Built for growers", "A simpler path from coordinates to informed decisions."],
] as const;

export function HomePage() { return <PublicShell><main><section className="relative overflow-hidden bg-gradient-to-br from-emerald-600 via-emerald-700 to-green-950 text-white"><div className="absolute inset-0 opacity-20 [background-image:radial-gradient(white_1px,transparent_1px)] [background-size:22px_22px]"/><div className="relative mx-auto grid max-w-7xl gap-12 px-5 py-20 md:py-28 lg:grid-cols-[1.15fr_.85fr] lg:items-center"><div><span className="inline-flex rounded-full border border-white/25 bg-white/10 px-3 py-1 text-xs font-semibold text-emerald-50">Climate intelligence for agriculture</span><h1 className="mt-6 max-w-3xl text-5xl font-bold leading-tight tracking-tight md:text-6xl">Better farm decisions, built from <span className="text-emerald-200">real signals.</span></h1><p className="mt-6 max-w-xl text-lg leading-8 text-emerald-50">TerraScore brings satellite vegetation, land-surface temperature, climate indicators, and credit risk into one clear farming workspace.</p><div className="mt-8 flex flex-wrap gap-3"><Link className="inline-flex items-center gap-2 rounded-xl bg-white px-5 py-3 font-semibold text-emerald-700 shadow-lg transition hover:bg-emerald-50" to="/register">Get started free <ArrowRight size={17}/></Link><Link className="rounded-xl border-2 border-white/60 px-5 py-3 font-semibold text-white transition hover:bg-white/10" to="/features">Explore features</Link></div></div><div className="rounded-3xl border border-white/25 bg-white/10 p-6 shadow-2xl backdrop-blur-sm"><div className="flex items-center justify-between"><div><p className="text-sm text-emerald-100">TerraScore signal overview</p><p className="mt-2 text-5xl font-bold">82.4</p><p className="mt-1 text-sm text-emerald-100">Sample credit score</p></div><div className="rounded-2xl bg-white/15 p-4"><Leaf className="text-emerald-100" size={34}/></div></div><div className="mt-7 grid grid-cols-2 gap-3">{[["NDVI", "0.71"], ["LST", "27.8°C"], ["Risk", "Moderate"], ["Climate", "Stable"]].map(([label, value]) => <div key={label} className="rounded-2xl bg-white/10 p-4"><p className="text-xs text-emerald-100">{label}</p><p className="mt-1 font-semibold">{value}</p></div>)}</div></div></div></section><section className="mx-auto max-w-7xl px-5 py-16"><div className="mb-10 text-center"><span className="text-sm font-semibold text-emerald-700">One connected platform</span><h2 className="mt-2 text-3xl font-bold text-emerald-950 md:text-4xl">Everything you need for farm intelligence</h2><p className="mx-auto mt-3 max-w-2xl text-slate-600">The Agrishield-inspired experience makes TerraScore’s existing capabilities easier to explore, without changing what happens behind the scenes.</p></div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{features.map(([Icon, title, text]) => <div key={title} className="group rounded-2xl border border-emerald-100 bg-white p-6 shadow-lg shadow-emerald-950/5 transition hover:-translate-y-1 hover:shadow-xl"><div className="mb-5 w-fit rounded-2xl bg-emerald-100 p-3 text-emerald-700 transition group-hover:bg-emerald-600 group-hover:text-white"><Icon size={23}/></div><h3 className="text-lg font-bold text-emerald-950">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{text}</p></div>)}</div></section><section className="bg-gradient-to-r from-emerald-600 to-emerald-700 px-5 py-16 text-white"><div className="mx-auto max-w-4xl text-center"><h2 className="text-3xl font-bold md:text-4xl">Ready to understand your farm data?</h2><p className="mx-auto mt-4 max-w-2xl text-emerald-100">Create an account to manage farms, run predictions, download reports, and track agricultural risk in one secure workspace.</p><Link className="mt-8 inline-flex items-center gap-2 rounded-xl bg-white px-6 py-3 font-semibold text-emerald-700 shadow-lg transition hover:bg-emerald-50" to="/register">Create your account <ArrowRight size={17}/></Link></div></section></main></PublicShell>; }

export function AboutPage() { return <PublicShell><Content title="About TerraScore" lead="Agricultural intelligence that respects the complexity of every growing season."><p>TerraScore helps users understand crop-health, climate, drought, and credit signals in one secure workspace. It is designed to make the backend’s existing ML predictions easier to use—not to replace agronomic expertise.</p></Content></PublicShell>; }
export function FeaturesPage() { return <PublicShell><Content title="Built for farm intelligence" lead="One connected workflow from coordinates to actionable signals."><div className="grid gap-4 md:grid-cols-2">{["ML prediction results with NDVI, LST, risk, and credit score", "Farm management with interactive coordinate maps", "PDF reports and batch CSV/XLSX prediction uploads", "Forecast, prediction history, analytics, and background training"].map((feature) => <div key={feature} className="flex gap-3 rounded-xl border border-emerald-100 bg-white p-4 text-sm text-slate-700 shadow-sm"><CheckCircle2 className="shrink-0 text-emerald-600" size={18}/>{feature}</div>)}</div></Content></PublicShell>; }
export function ContactPage() { return <PublicShell><Content title="Contact" lead="Have questions about TerraScore? Reach the project team through your configured support channel."><div className="flex items-center gap-3 rounded-xl border border-emerald-100 bg-white p-5 text-slate-700 shadow-sm"><Mail className="text-emerald-600"/><span>A contact-submission endpoint is not currently exposed by the FastAPI backend, so this site does not fabricate a contact form.</span></div></Content></PublicShell>; }
export function FaqPage() { return <PublicShell><Content title="Frequently asked questions" lead="Clear answers about the TerraScore workflow."><div className="space-y-3">{[["What powers a score?", "The backend combines its trained model with satellite and climate-derived inputs."], ["Can I link predictions to a farm?", "Yes. Save a farm, then select it when submitting a prediction."], ["Where can I download a report?", "The Reports page lists saved predictions with server-generated PDF downloads."], ["Is weather data live?", "The dashboard requests the backend forecast endpoint for your first saved farm."]].map(([question, answer]) => <details key={question} className="rounded-xl border border-emerald-100 bg-white p-4 shadow-sm"><summary className="cursor-pointer font-medium text-emerald-950">{question}</summary><p className="mt-3 text-sm leading-6 text-slate-600">{answer}</p></details>)}</div></Content></PublicShell>; }
function Content({ title, lead, children }: { title: string; lead: string; children: React.ReactNode }) { return <main className="mx-auto max-w-3xl px-5 py-20"><span className="text-sm font-semibold text-emerald-700">TerraScore website</span><h1 className="mt-3 text-4xl font-bold tracking-tight text-emerald-950">{title}</h1><p className="mt-4 text-lg leading-8 text-slate-600">{lead}</p><section className="mt-10 space-y-5 text-slate-700">{children}</section></main>; }
