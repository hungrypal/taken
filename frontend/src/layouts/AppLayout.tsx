// This responsive shell provides the shared SaaS sidebar, header, navigation, and account menu.
import { BarChart3, BrainCircuit, FileText, LayoutDashboard, Leaf, LogOut, Menu, Settings, Sprout, UserCircle, X } from "lucide-react";
import { useState } from "react";
import { NavLink, Outlet, useNavigate } from "react-router-dom";
import { useAuth } from "../context/AuthContext";
const links = [{ to: "/dashboard", label: "Dashboard", icon: LayoutDashboard }, { to: "/predictions", label: "Predictions", icon: BrainCircuit }, { to: "/farms", label: "Farms", icon: Sprout }, { to: "/reports", label: "Reports", icon: FileText }, { to: "/training", label: "Model Training", icon: BrainCircuit }, { to: "/analytics", label: "Analytics", icon: BarChart3 }, { to: "/settings", label: "Settings", icon: Settings }];
export function AppLayout() { const [open, setOpen] = useState(false); const { user, logout } = useAuth(); const navigate = useNavigate(); const sidebar = <aside className="flex h-full w-72 flex-col border-r border-emerald-100 bg-white p-4"><div className="mb-8 flex items-center gap-3 px-2"><span className="rounded-2xl bg-emerald-600 p-2.5 text-white shadow-lg shadow-emerald-600/25"><Leaf size={22} /></span><div><p className="text-xl font-bold text-slate-800">TerraScore</p><p className="text-xs text-emerald-700">Smart farm intelligence</p></div></div><nav className="space-y-1">{links.map(({ to, label, icon: Icon }) => <NavLink key={to} to={to} end={to === "/dashboard"} onClick={() => setOpen(false)} className={({ isActive }) => `flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-medium transition ${isActive ? "bg-emerald-600 text-white shadow-md shadow-emerald-600/15" : "text-slate-600 hover:bg-emerald-50 hover:text-emerald-800"}`}><Icon size={18} />{label}</NavLink>)}</nav><div className="mt-auto border-t border-emerald-100 pt-4"><NavLink to="/profile" className="flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm text-slate-600 hover:bg-emerald-50"><UserCircle size={18} />Profile</NavLink><button onClick={() => { logout(); navigate("/login"); }} className="flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-sm text-slate-500 hover:bg-red-50 hover:text-red-600"><LogOut size={18} />Logout</button></div></aside>; return <div className="min-h-screen bg-[#f7faf7] text-slate-800"><div className="hidden lg:fixed lg:inset-y-0 lg:flex">{sidebar}</div>{open && <div className="fixed inset-0 z-50 flex lg:hidden"><div className="fixed inset-0 bg-slate-950/30" onClick={() => setOpen(false)} /><div className="relative shadow-2xl">{sidebar}<button className="absolute right-3 top-4 text-slate-500" onClick={() => setOpen(false)} aria-label="Close navigation"><X /></button></div></div>}<main className="lg:pl-72"><header className="sticky top-0 z-20 flex h-20 items-center justify-between border-b border-emerald-100 bg-white/90 px-4 backdrop-blur lg:px-8"><button className="rounded-lg p-2 text-slate-700 hover:bg-emerald-50 lg:hidden" onClick={() => setOpen(true)} aria-label="Open navigation"><Menu /></button><div className="hidden max-w-md flex-1 lg:block"><input aria-label="Search" placeholder="Search farms or predictions…" className="field m-0 py-2.5" /></div><NavLink to="/profile" className="flex items-center gap-2.5 text-sm text-slate-700"><span className="grid h-9 w-9 place-items-center rounded-full bg-emerald-100 font-bold text-emerald-800">{user?.full_name?.[0] ?? user?.email[0]?.toUpperCase()}</span><span className="hidden sm:block">{user?.full_name ?? user?.email}</span></NavLink></header><div className="mx-auto max-w-7xl p-4 md:p-8"><Outlet /></div><footer className="border-t border-emerald-100 px-8 py-6 text-center text-xs text-slate-500">TerraScore · Secure agricultural intelligence</footer></main></div>; }
